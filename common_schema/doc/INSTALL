The file is a SQL source file. To install it, you should execute it on your MySQL server.

Either:

- Within MySQL, issue:
mysql> SOURCE '/path/to/file.sql';

- From shell, execute:
bash$ mysql < '/path/to/file.sql'

- Use your favorite MySQL GUI editor, copy+paste file content, execute.
